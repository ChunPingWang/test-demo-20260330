meta {
  name: 成功刪除存在的客戶
  type: http
  seq: 1
}

vars {
  base_url: {{BASE_URL}}
}

DELETE {{base_url}}/api/customers/1
Content-Type: application/json

tests {
  test("HTTP 狀態碼應為 204", function() {
    expect(res.status).to.equal(204);
  });
  
  test("回應體應該為空", function() {
    expect(res.body).to.be.empty;
  });
}
